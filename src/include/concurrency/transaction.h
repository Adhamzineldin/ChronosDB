#pragma once
namespace francodb {
    // A placeholder for future transaction management
    class Transaction {
    public:
        Transaction(int txn_id) {
            (void)txn_id;
        }
    };
}